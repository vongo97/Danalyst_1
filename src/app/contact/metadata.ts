import { Metadata } from 'next';
import { siteConfig } from '@/config/site';

export const metadata: Metadata = {
  title: `Contacto | ${siteConfig.name}`,
  description: `Ponte en contacto con ${siteConfig.name} para consultas y soporte.`,
};