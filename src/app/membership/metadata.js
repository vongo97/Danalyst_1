import { siteConfig } from '@/config/site';

export const metadata = {
  title: `Membresía Premium | ${siteConfig.name}`,
  description: `Únete a la membresía premium de ${siteConfig.name} y transforma tu carrera en análisis de datos.`,
};